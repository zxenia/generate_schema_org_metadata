# Dataset metadata to schema.org json-ld metadata

Generates schema.org json-ld metadata for dataset

Json result can be tested in [Google Rich Results Test](https://search.google.com/test/rich-results)